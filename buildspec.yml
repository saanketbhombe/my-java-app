version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - echo "Building Maven project..."
      - mvn clean package

artifacts:
  files:
    - target/*.jar
